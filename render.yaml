services:
  - type: web
    name: cookie-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --bind 0.0.0.0:10000 --timeout 120 cookie_service:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
