services:
  - type: web
    name: ssvid-cookie-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --bind 0.0.0.0:8080 --timeout 120 cookie_service:app
